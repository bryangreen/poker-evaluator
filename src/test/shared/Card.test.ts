import {expect} from "chai";


describe('add-card', ()=> {



  it('should work!', () => {
    expect(true).to.be.true;

  })
  it('should work2!', () => {
    expect(true).to.be.true;

  })
  it('should work3!', () => {
    expect(true).to.be.true;

  })
  it('should work4!', () => {
    expect(true).to.be.true;

  })

});
